'use strict';

/**
 * Vendor JS
 *
 * Require all vendor-specific libraries in this file. It is
 * kept separate from application-specific scripts because
 * these should change less frequently and should be able to
 * be cached by the client browser for longer.
 */

require('angular/angular');
require('angular-resource/angular-resource');
require('angular-ui-router/release/angular-ui-router');
